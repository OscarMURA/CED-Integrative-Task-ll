package com.example.romeandvikings.model;

/** The code snippet is defining an enumeration called `Implementation`. An enumeration is a special
* type in Java that represents a fixed set of constants. In this case, the `Implementation`
* enumeration has two constants: `ADJACENCY_MATRIX` and `ADJACENCY_LIST`. These constants can be used
* to represent different implementations in a program, such as different data structures or
* algorithms. */
public enum Implementation {
    ADJACENCY_MATRIX,
    ADJACENCY_LIST
}
